<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TimetableApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/darkred</item>
    </style>

    <style name="Theme.TimetableApplication" parent="Base.Theme.TimetableApplication" />

    <!-- Correct theme for HomeActivity -->
    <style name="Theme.HomeActivity" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">#E0E0E0</item> <!-- Light grey status bar -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item> <!-- Use dark icons on light status bar -->
    </style>

</resources>